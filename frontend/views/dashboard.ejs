<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/header') %>
  <%# Bootstrap CSS from CDN for styling %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <%# Custom CSS for dashboard styling %>
  <link rel="stylesheet" href="/css/styles.css" />
  <title>Chamama Greenhouse Dashboard</title>
</head>

<body>

  <%# Navbar using Bootstrap classes %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary px-4">
    <span class="navbar-brand fs-4 fw-bold">
      ❄️ Chamama Greenhouse - Eastern Wing
    </span>
    <div class="ms-auto">
      <a href="/" class="btn btn-dark me-2">Home</a>
      <a href="#" class="btn btn-warning fw-bold">View Charts</a>
    </div>
  </nav>


  <%# Main container for dashboard content %>
  <main class="container py-5">
    <div class="row">
      <%# Left side with title and description %>
      <div class="col-md-7 d-flex flex-column justify-content-center text-white">
        <h1 class="fw-bold display-5 mb-3">IoT Greenhouse Monitoring</h1>
        <p class="lead">
          Monitor Temperature, Moisture, and Humidity in real-time with accurate live updates.
        </p>
      </div>
      <%# Right side card displaying live data %>
      <div class="col-md-5">
        <div class="card text-white rounded-4 shadow live-data-card" style="background: rgba(27,44,72,0.88);">
          <div class="card-body">
            <h3 class="card-title mb-4">Chamama Greenhouse - Live Data</h3>

            <%# Row for temperature, moisture, humidity %>
            <div class="d-flex justify-content-between mb-4" id="main-stats-row">
              <%# Temperature %>
              <div class="text-center flex-fill">
                <div style="font-size:2.2rem;">&#x1F321;</div>
                <div class="fs-2 fw-bold" id="temperature-value">--°C</div>
                <span class="text-info">Temperature</span>
              </div>
              <%# Moisture %>
              <div class="text-center flex-fill">
                <div style="font-size:2.2rem;">&#x1F4A7;</div>
                <div class="fs-2 fw-bold" id="moisture-value">--%</div>
                <span class="text-info">Moisture</span>
              </div>
              <%# Humidity %>
              <div class="text-center flex-fill">
                <div style="font-size:2.2rem;">&#x1F30A;</div>
                <div class="fs-2 fw-bold" id="humidity-value">--%</div>
                <span class="text-info">Humidity</span>
              </div>
            </div>

            <%# Separator line %>
            <hr />
            <%# Container for hourly data blocks inserted dynamically %>
            <div id="hourly-data" class="d-flex flex-wrap justify-content-between"></div>
          </div>
        </div>
      </div>
    </div>
  </main>


        